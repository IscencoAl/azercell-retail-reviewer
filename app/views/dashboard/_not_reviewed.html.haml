.table-responsive{:style => 'max-height:355px;'}
  %table.table.table-condensed.table-striped.table-vcentered
    %thead
      %tr
        %th= t('activerecord.attributes.report.shop')
        %th.text-right= t('activerecord.attributes.report.user')

    %tbody
      -if shops.any?
        - shops.each do |shop|
          %tr
            %td= can_link_to(shop.name, shop)

            %td.text-right
              - if shop.user
                = can_link_to(shop.user.full_name, shop.user)
              - else
                = '-'

      - else
        %tr
          %td{:colspan => 2}
            %h4.text-center= t('views.common.empty')