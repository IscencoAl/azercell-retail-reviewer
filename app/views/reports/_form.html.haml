= simple_form_for(@report) do |f|
  = f.error_notification

  .form-inputs
    = f.input :created_at
    = f.input :latitude
    = f.input :longitude
    = f.association :user
    = f.association :shop
    = f.input :score

  .form-actions
    = f.button :submit
