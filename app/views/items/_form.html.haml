= simple_form_for @item, :html => {:class => 'form-horizontal', :role => 'form'} do |f|

  = f.input :name

  .btn-toolbar.text-center
    = f.button :submit, t('views.common.save')
    %a.btn.btn-default{:href => session[:return_to] || items_path}= t('views.common.cancel')