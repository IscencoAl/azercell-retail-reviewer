= simple_form_for @region, :html => {:class => 'form-horizontal', :role => 'form'} do |f|

  = f.input :name

  .btn-toolbar.text-center
    = f.button :submit, t('views.common.save'), :class => 'btn-primary'
    %a.btn.btn-default{:href => regions_path}= t('views.common.cancel')