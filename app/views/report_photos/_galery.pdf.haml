.galery-wrapper.panel.panel-default{:class => "#{"hidden" if photos.blank?}"}
  .panel-body
    .galery.row
      - photos.each do |p|
        .col-xs-3.photo
          %a.thumbnail{:href => '#'}
            = wicked_pdf_image_tag p.photo, :class => 'img.responsive'